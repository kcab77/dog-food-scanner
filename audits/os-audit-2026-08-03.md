# OS Audit — 2026-08-03

**Knowledge current through: 2026-07-28** (ingested layer — newest `commonsensedog knowledge/` note
and newest `qa-*.json` pack both land on that date). The raw layer is fresher: transcripts synced
today. Six days of drift between pulled and ingested is healthy.

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity  | 🔴 RED    | The manual's documented fallback (`common-sense-dog-ai-backup/`) holds a **different, older** `pinecone.ts` — no 429 retry |
| Index truth        | 🔴 RED    | `MEMORY.md` indexes `hershey_tick_story.md`, which does not exist anywhere |
| Freshness          | 🟡 YELLOW | `knowledge-vault/` frozen since 2026-06-14; 13 notes never ingested anywhere |
| Bloat/duplication  | 🔴 RED    | `common-sense-dog-ai-backup/` — 74 MB, **tracked**, and drifted from live |
| Hygiene            | 🟢 GREEN  | Secrets clean, agents load, hook wired — nothing broken |
| Context placement  | 🟡 YELLOW | Live counters baked into always-loaded files; no precedence rule between the two `CLAUDE.md`s |

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false)            | **HIGH** | `docs/TODO.md` claims "Last updated 2026-06-24" but was edited 2026-08-02; backup scripts present as interchangeable when they aren't; hardcoded counters in preloaded files |
| Bloat (too much)             | MED      | 74 MB tracked duplicate; ~4,270 words loaded every session across three always-on files; 403 MB `_archive/` |
| Confusion (wrong or missing) | **HIGH** | 18 real root directories unmapped in the manual; phantom memory row; `knowledge-vault/` is dog knowledge invisible to every retrieval path |
| Clash (contradictory)        | **HIGH** | Live vs backup copies of `pinecone.ts`, `seed-pinecone.mjs`, `ingest_pack.js` differ, and the manual routes you to the backup |

## Since last audit

First run — no prior report. Note for next time: today's vault reorganization (two-brain split)
happened hours before this audit, so routing/index numbers on the **vault** side read better than
the long-run baseline. Run two will be the honest comparison.

## What would make your agent wrong-answer you today

- **The backup fallback is a trap.** `CLAUDE.md` says: if the main folder's keys or scripts are
  missing, run ingestion from `common-sense-dog-ai-backup/`. That copy's `lib/pinecone.ts` is 116
  lines vs the live 99 and **lacks the 429 retry/backoff** added for the Voyage rate limit; its
  `seed-pinecone.mjs` is 364 lines vs 187. Following the documented instruction would silently use
  different embedding behavior than production — and nothing would error.
- **A memory that doesn't exist is indexed as if it does.** `MEMORY.md` has 30 rows for 29 files;
  `hershey_tick_story.md` is listed but absent from both the memory folder and the vault. An agent
  that trusts the index will assert Hershey's tick story is on file and then find nothing.
- **`docs/TODO.md` lies about its own age.** Footer says 2026-06-24; four items were added
  2026-08-02. Anything reasoning about "what's recent" reads a six-week-old date on a file edited
  yesterday. (My edit — I added the items and didn't touch the footer.)
- **13 dog-health notes are invisible.** `knowledge-vault/` is untracked, gitignored, unmapped in
  the manual, and frozen at 2026-06-14. Real content (xylitol toxicity, isoxazoline reactions, tick
  attachment windows) that is in neither Obsidian nor Pinecone. No retrieval path reaches it.

## Findings by check

### Check 1 — Routing integrity 🔴

- **Misroute:** `CLAUDE.md` → `common-sense-dog-ai-backup/` as the ingest fallback. Files differ:
  `lib/pinecone.ts` (99 vs 116 lines), `scripts/seed-pinecone.mjs` (187 vs 364),
  `scripts/ingest_pack.js` (98 vs 97). `process_content.mjs` and `blog-data.ts` are identical.
  The backup also has **no** `lib/answers-data.ts` — the 50 live SEO pages don't exist there. `[clash]` `[poisoning]`
- **18 unmapped root directories.** The structure tree lists only `app/`, `lib/`,
  `common-sense-dog-ai/`, `docs/`. Real and unlisted: `agent-os/`, `brain-os/`, `daily-brief/`,
  `drafts/`, `knowledge-vault/`, `pet-store-chat-agent/`, `scripts/`, `supabase/`, `components/`,
  `constants/`, `hooks/`, `storage/`, `memory/`, `assets/`, `credentials/`, `_archive/`,
  `_icloud-duplicates/`, and `expert-platform/` (mentioned in prose today, still absent from the
  tree). `[confusion]`
- **Ambiguous path:** the tree shows `lib/blog-data.ts` as if at repo root; it lives at
  `common-sense-dog-ai/lib/blog-data.ts`. Root `lib/` is the app's Supabase layer. `[confusion]`
- ✅ All six bare filenames in prose (`ingest_pack.js`, `process_content.mjs`, `seed-pinecone.mjs`,
  `seed-blog-content.mjs`) resolve under `common-sense-dog-ai/scripts/`. Hook path resolves.
- ✅ All three agent files carry valid descriptions and models. No dead paths inside them.

### Check 2 — Index truth 🔴

- **Phantom:** `MEMORY.md` row → `hershey_tick_story.md`. Absent from the memory folder *and* from
  `Brain/claude-memory/dog-food-scanner/`. 30 rows / 29 files. No orphans in the other direction. `[confusion]`
- **False freshness claim:** `docs/TODO.md` footer `Last updated: 2026-06-24`, actual last edit
  2026-08-02. `[poisoning]`
- `README.md` last committed 2026-02-24 — five months untouched while the app went 1.7 → 1.9. Not
  read by agents, so cosmetic rather than dangerous; ranked last.
- ✅ Counter claims that check out: app version `v1.9.0` matches `app.json`; `commonsensedog
  knowledge/` = 34 notes as stated; `claude-memory` = 29 as stated; "zero hex literals in
  `app/index.tsx`" verified at **0**.
- ⚠️ One counter off by one: vault's *Four Areas* says "49 answer pages"; actual is **50**.

### Check 3 — Freshness 🟡

| Feed | Raw date | Ingested date | Cadence | Verdict | What's missing |
|---|---|---|---|---|---|
| Session transcripts (SessionEnd hook) | 2026-08-03 | 2026-08-03 | per-session | **FRESH** | — |
| Claude memory → vault | 2026-08-03 | 2026-08-03 | per-session | **FRESH** | — |
| `commonsensedog knowledge/` notes | 2026-07-28 | 2026-07-28 | on-demand | **ON-DEMAND** | not stale by definition |
| `qa-*.json` packs → Pinecone | 2026-07-28 | 2026-07-28 | on-demand | **ON-DEMAND** | 58 packs; 11 items still unchecked in `PINECONE_TODO.md` |
| `raw/research/` → wiki/Pinecone | 2026-07-30 | 2026-07-29 | on-demand | **DRIFTING** | 2 files pulled, never ingested (batch 3, trending topics) |
| `knowledge-vault/` | 2026-06-14 | **never** | unknown | **RETIRED?** | 13 notes, 7 weeks cold, no ingest path |

- **Memory staleness scan:** `project_pawgrade_app_status.md` says "v1.7.0 status: pending App Store
  submission" — the app is now v1.9.0 and live. `project_petchat_followup.md` says "send 2026-03-25",
  four months past. `project_upc_upgrade.md` is a TODO that `docs/TODO.md` also carries. `[poisoning]`
- ✅ No mtime-cluster artifact detected; dated filenames and git log agree.

### Check 4 — Bloat, duplication, organization 🔴

- **`common-sense-dog-ai-backup/`** — 196 files, **74 MB, tracked in git**, last committed
  2026-07-30. A full second copy of the website that has diverged from live (see Check 1). Its
  stated purpose is holding intact `.env.local` keys after the iCloud incident. Keeping the *keys*
  is reasonable; keeping a drifting 74 MB code duplicate in version control is where the clash
  comes from. `[clash]` `[bloat]`
- **`_archive/`** — 8,429 files / 403 MB, correctly gitignored. Fine where it is; noted for scale.
- **`_icloud-duplicates/`** — 23 files, gitignored. Name states its own purpose; nothing routes to it. `[bloat]`
- **Root hygiene** — 17 loose files. Most are legitimately root-level config (`package.json`,
  `app.json`, `eas.json`, `tsconfig*.json`, `vercel.json`, `eslint.config.js`, `expo-env.d.ts`).
  Three are re-homing candidates, all **move-with-caution** until references are grepped:
  - `credentials.json` → `credentials/` already exists and is gitignored
  - `privacy-policy.html` → no obvious home; likely referenced by the App Store listing
  - `start-dashboard.sh` → referenced by `_Skills Map.md` in the vault; moving breaks that row
- **Always-loaded weight:** repo `CLAUDE.md` 2,290 words + vault `CLAUDE.md` 1,338 + `MEMORY.md` 643
  ≈ **4,271 words on every session**. Not alarming, worth watching — the repo manual grew today. `[bloat]`
- **Intuitiveness gut check** — walked to three artifacts as a human would:
  - *"the scoring rules"* → `docs/APP_SPEC.md` §3 ✅ obvious
  - *"Hershey's protocol"* → `Brain/People/` in the vault ✅ obvious
  - *"the Dr. Judy pitch"* → was `wiki/business/assets/`, now `wiki/expert-platform/assets/` ✅ (fixed today)
  - ❌ **`knowledge-vault/` fails the test entirely** — the name promises the knowledge base, but the
    real one is Pinecone plus the Obsidian vault. A human *or* an agent would open it expecting
    authority and get 13 orphaned notes.

### Check 5 — Hygiene 🟢

- ✅ **Secrets clean.** `credentials.json`, `.env`, `common-sense-dog-ai/.env.local`,
  `expert-platform/.env`, `expert-platform/.env.local` — all **untracked and gitignored**. Only
  `expert-platform/.env.example` is tracked, which is correct.
- ✅ `_archive/`, `_icloud-duplicates/`, `knowledge-vault/`, `credentials/` all gitignored — no risk
  of a stray `git add -A` sweeping 400 MB or credentials into history.
- ✅ All three agents have non-empty descriptions and a valid model (`sonnet`). None reference dead paths.
- ✅ `os-audit` skill file is correctly named `SKILL.md` with valid frontmatter.
- ⚠️ **Cadence reality check: zero scheduled jobs.** `crontab` is empty. The only automation is the
  SessionEnd hook (verified present and wired in `settings.json`). Every "recurring" process —
  ingestion, research, audits — is manual. This is the root cause of the Check-3 drift above, and
  the reason "weekly audit" will only happen if you say the words.

### Check 6 — Context placement 🟡

- **Situational facts baked into the always-loaded layer** — each will go stale in a file the agent
  always trusts, while costing tokens every session. Fix is a pointer, not a fresher copy:
  - `CLAUDE.md`: `v1.9.0`, "(currently 0 — keep it 0)" hex count, "all 495 were migrated",
    "34 notes", `PINECONE_TODO` counts. `[poisoning]` `[bloat]`
  - Vault *Four Areas*: "49 answer pages + 34 A–Z topics" — already off by one.
- **Expertise buried in situational stores:** the `.easignore` / `EXPO_PUBLIC_APP_SECRET` 403 rule
  and the "never `.easignore` an app code dir" rule live in `_Skills Map.md` in the vault — correct
  and load-bearing, but a fresh session in the repo won't see it unless routed. It *is* also in the
  repo manual, so this one is covered.
- **No precedence rule.** Two `CLAUDE.md` files (repo + vault) and no line anywhere saying which
  wins when they disagree. Nothing conflicts today; the rule is missing before it's needed. `[clash]`
- **Spot-checked 5 duplicated facts:** app version ✅ consistent · hex-literal count ✅ accurate ·
  `commonsensedog knowledge` count ✅ · memory count ✅ · answer-page count ❌ (49 vs 50, two ages).

## Questions for you

1. **`knowledge-vault/` — did you stop this on purpose?** 13 notes, frozen 2026-06-14, no ingest
   path. Three options: fold into Pinecone/Obsidian, archive it, or label it a dead experiment. It
   should not stay as-is with an authoritative name.
2. **Do you still need `common-sense-dog-ai-backup/` as a full code copy?** The keys are the part
   that matters. Keeping only `.env.local` outside git would kill the 74 MB, the drift, and the
   misroute in one move.
3. **`_icloud-duplicates/` and `_archive/` (403 MB)** — safe to leave, or do you want them off the
   working tree entirely?
4. **Is `privacy-policy.html` referenced by your App Store listing?** Determines whether it can move.

## Fix list (batched, await approval)

**Batch A — correctness in the always-loaded layer (do first; these cause wrong answers today)**
- Remove or restore the `hershey_tick_story.md` row in `MEMORY.md` (the content exists in this
  conversation's history — it can be rewritten rather than dropped).
- Fix the `docs/TODO.md` footer date.
- Rewrite the backup-fallback instruction in `CLAUDE.md` to say what's actually true: the backup is
  a **key store**, its scripts have drifted, don't run ingestion from it.
- Refresh the three stale memory notes (v1.7.0 status, the March follow-up, the UPC TODO).

**Batch B — routing and index reconciliation**
- Add the 18 unmapped directories to `CLAUDE.md`'s structure tree, one line each.
- Disambiguate `lib/blog-data.ts` → `common-sense-dog-ai/lib/`.
- Replace hardcoded counters with pointers ("run `sync-library.mjs` for the current count").
- Add one precedence line: repo `CLAUDE.md` wins on code; vault `CLAUDE.md` wins on knowledge.
- Decide `knowledge-vault/`'s fate per your answer above.

**Batch C — data catch-up**
- Ingest the two waiting `raw/research/` files (batch 3, trending topics).
- Work down the 11 unchecked `PINECONE_TODO.md` items.
- Resolve the mast-cell-tumor holistic-vs-safety decision that's been blocking since 2026-07-29.

**Batch D — durability (so it can't silently freeze again)**
- Nothing here is scheduled. A weekly `/os-audit` is a ritual, not a cron — consider whether you
  want an actual scheduled job, or accept that it fires when you ask.
- Consider a pre-commit or SessionEnd check that fails when a "Last updated" footer disagrees with
  the file's real mtime. That single class of error caused two findings in this report.

---
*Read-only audit. Nothing was changed. Re-run after any big reorganization, or weekly per Kyle's
standing preference.*
