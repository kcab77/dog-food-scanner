# OS Audit — 2026-09-04

**Knowledge current through: 2026-08-29** (last Pinecone ingest, `qa-leaky-gut`, verified retrievable).
The knowledge base itself is fresh. **Its tracker is not** — see Index truth.

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity  | 🟢 GREEN  | Every path in `CLAUDE.md` resolves, both directions. Only `node_modules/` is unmapped, which is correct. |
| Index truth        | 🔴 RED    | `PINECONE_TODO.md` "Already in Pinecone" omits 4 of the 5 packs added since 3 Aug |
| Freshness          | 🟡 YELLOW | Six top-level projects dormant 43–126 days; `PINECONE_TODO.md` a month behind |
| Bloat/duplication  | 🟡 YELLOW | 759 MB of inert bulk; 26 loose files at repo root |
| Hygiene            | 🟢 GREEN  | No secrets tracked. `.env`, `credentials/`, `credentials.json` all ignored |
| Context placement  | 🟡 YELLOW | `CLAUDE.md` is 3,760 words, paid on every single session |

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false)            | **MED**  | `PINECONE_TODO.md` presents a stale list as the record of what's ingested |
| Bloat (too much)             | **MED**  | 3,760-word always-loaded `CLAUDE.md`; 759 MB inert; 26 root files |
| Confusion (wrong or missing) | **MED**  | Same tracker — a session could re-ingest packs that already exist, or conclude a topic is uncovered when it is |
| Clash (contradictory)        | **LOW**  | Precedence rule exists and is followed; the label-band drift found on 1 Sept is already fixed |

## Since last audit (2026-08-03)

**Fixed:** memory index is now perfect (36 indexed / 36 on disk, zero phantoms, zero orphans — it had drifted before). Secrets hygiene is clean; the live Anthropic key found in `drafts/html/pawgrade-web.html` on 27 Aug was removed and the key confirmed dead. 46 MB of source PNGs untracked and gitignored. Two iCloud `" 2"` duplicates removed and the pattern ignored. A GitHub workflow that had failed on every push since May was made manual-only.

**Worse:** `PINECONE_TODO.md` has fallen a month behind while five packs were added. `_archive/` grew to 409 MB.

**New:** `expert-platform/` is now 424 MB and active (6 days). `scripts/score-check.mjs` added 1 Sept — a scoring regression harness that found a live bug on its first run.

## What would make your agent wrong-answer you today

- **"Do we have a pack on mushrooms / omegas / gut probiotics?"** → `PINECONE_TODO.md` says no. All three are ingested and retrievable. A session trusting the tracker would waste an afternoon re-writing 70+ Q&A pairs that already exist.
- **"What's the score label for a 95?"** — *was* wrong until 1 Sept (three competing label functions, the app showed "Good"). Fixed, and now guarded by `scripts/score-check.mjs`.
- Nothing else. Routing, memory index, and secrets are all clean.

## Findings by check

### 1 · Routing integrity — GREEN
Every path named in `CLAUDE.md` resolves: 10 vault paths, 9 `common-sense-dog-ai` paths, 11 `docs/` files. Reverse direction clean — every top-level directory except `node_modules/` is mapped. No dead routes, no misroutes.

### 2 · Index truth — RED
- `PINECONE_TODO.md` § "Already in Pinecone" is missing `qa-mushrooms`, `qa-omegas`, `qa-gut-probiotics`, `qa-leaky-gut`. 70 `qa-*.json` packs on disk; the tracker was last touched 2026-08-03. **[poisoning] [confusion]**
- `MEMORY.md`: 36 indexed, 36 on disk, no phantoms, no orphans. ✅

### 3 · Freshness — YELLOW
| Feed / project | Last activity | Verdict |
|---|---|---|
| `common-sense-dog-ai/` (site + KB) | 2 days | FRESH |
| `expert-platform/` | 6 days | FRESH |
| `scripts/` | 2 days | FRESH |
| `drafts/` | 8 days | FRESH |
| `storm-reports/` | 15 days | ON-DEMAND — not stale by nature |
| `supabase/` | 43 days | ON-DEMAND (migrations) |
| `pet-store-chat-agent/` | 49 days | **RETIRED?** |
| `agent-os/` `brain-os/` `daily-brief/` | 53 days | **RETIRED?** |
| `common-sense-dog-ai-backup/` | 53 days | Key store by design — correct that it's frozen |
| `components/` `storage/` `constants/` `hooks/` | 106–126 days | Expo scaffolding, "largely unused" per `CLAUDE.md` |

`docs/TODO.md` claims "last updated 2026-08-22", modified 2026-08-23 — one day, trivial. **[poisoning, minor]**

### 4 · Bloat, duplication and organisation — YELLOW
- **759 MB inert:** `_archive/` 409 MB · `common-sense-dog-ai-backup/` 350 MB. Both gitignored, neither read by anything. **[bloat]**
- **`node_modules/` 586 MB + `expert-platform/` 424 MB** — normal for two Node projects, noted for disk awareness only.
- **26 loose files at repo root**, including 8 PNGs and 1 PDF (all gitignored, but they still clutter the root a human has to navigate). The root should read like a table of contents. **[bloat]**
- `memory/` (4 KB, last touched 2026-06-06) is described in `CLAUDE.md` as legacy — confirmed dead. **[confusion]**
- **Intuitiveness check passed.** Walked to three recent artifacts — the yeast section (`app/index.tsx`), the leaky gut library note (vault → `sync-library.mjs` → `library-data.ts`), and the outreach template (`expert-platform/OUTREACH.md`) — each reachable by obvious folder names without searching.

### 5 · Hygiene — GREEN
- No secrets tracked. Only `expert-platform/.env.example` (a template, correct).
- `.env`, `credentials/`, `credentials.json` all gitignored and untracked.
- `.github/workflows/build-ios.yml` is now `workflow_dispatch` only — it had failed on every push since May, 29 emails in 7 days.
- ⚠️ **Cadence is entirely manual.** No hooks or scheduled jobs run anything in this repo. Every "recurring" process — Pinecone ingestion, library sync, doc updates — happens only when someone remembers. That is the root cause of the Index-truth RED above.

### 6 · Context placement — YELLOW
- **`CLAUDE.md` is 3,760 words**, loaded on every session. It is well-written and mostly durable *expertise* — but it also carries situational detail with a shelf life: specific score thresholds, specific 2026-08 change records, specific version behaviour. Those are the lines that will go stale in a file the agent always trusts. **[bloat] [future poisoning]**
- Precedence rule exists (§ 🥇) and is being followed — the 1 Sept label-band conflict was resolved by fixing the loser, not working around it. ✅
- Expert/dog brain separation is documented and holding. ✅

## Questions for you

1. **`agent-os/`, `brain-os/`, `daily-brief/`** — 53 days idle. Did you stop these on purpose, or are they meant to be running?
2. **`pet-store-chat-agent/` (PetChat)** — 49 days idle. Parked, or dead?
3. **`_archive/` at 409 MB** — anything in there you'd actually go back for, or is it disk you'd like returned?

## Fix list (batched, awaiting approval)

**Batch A — the RED, do first (~10 min)**
- Update `PINECONE_TODO.md` § Already in Pinecone with the 5 packs added since 3 Aug, and add the ingest date to each so staleness is visible next time.
- Fix `docs/TODO.md`'s one-day-off date claim.

**Batch B — root hygiene (~15 min)**
- Move the 8 source PNGs + 1 PDF out of the repo root into `drafts/infographics/` (they're gitignored, so this is purely navigational). Grep for references first — `CLAUDE.md` and the app reference the *compressed* copies in `assets/images/`, not these.
- Delete `memory/` (4 KB, legacy, superseded by `~/.claude/projects/.../memory/`) — or add a one-line README saying so.

**Batch C — durability, the actual cure (~30 min)**
- The Index-truth RED happened because nothing enforces the tracker. Add a `SessionEnd` hook, or a line in `PINECONE_PROTOCOL.md`, that makes updating `PINECONE_TODO.md` part of ingesting rather than a thing to remember.
- Consider the same for `scripts/score-check.mjs` — it only helps if it's run.

**Batch D — weight (discuss first)**
- `CLAUDE.md` at 3,760 words could shed the dated change-records (the 2026-07-19 / 08-14 / 08-23 scoring rationales) into `docs/APP_SPEC.md`, leaving pointers. Saves tokens every session and moves situational detail out of an always-loaded file. **Not recommended without discussion** — that history is genuinely load-bearing for scoring decisions.

---
*Read-only audit. Nothing was changed. Re-run after any big reorganisation.*
